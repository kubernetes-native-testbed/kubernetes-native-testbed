---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: point
  namespace: point
  labels:
    service: point
    role: app
spec:
  replicas: 3
  selector:
    matchLabels:
      service: point
      role: app
  template:
    metadata:
      labels:
        service: point
        role: app
    spec:
      containers:
        - name: point-app
          image: registry-harbor-core.infra.svc.cluster.local/library/point:4ffa52fb9e7ab0ce663f31840bd15e0a04b9bccd
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
              scheme: HTTP
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: point
  namespace: point
  labels:
    service: point
    role: app
spec:
  type: ClusterIP
  ports:
    - name: "http-port"
      protocol: "TCP"
      port: 8080
      targetPort: 8080
  selector:
    service: point
    role: app
