# Generated by below command
# #   git clone https://github.com/spotahome/redis-operator.git
# #   helm2 template --name infra --namespace infra --set rbac.install=true redis-operator/charts/redisoperator/ > redis-operator.yaml
---
# Source: redisoperator/templates/rbac.yaml

apiVersion: v1
kind: ServiceAccount
metadata:
  name: "infra-redisoperator"
  namespace: infra
  labels:
    app.kubernetes.io/name: "redisoperator"
imagePullSecrets:
  []
  
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: "infra-redisoperator"
  labels:
    app.kubernetes.io/name: "redisoperator"
rules:
  - apiGroups:
      - databases.spotahome.com
    resources:
      - redisfailovers
      - redisfailovers/finalizers
    verbs:
      - "*"
  - apiGroups:
      - apiextensions.k8s.io
    resources:
      - customresourcedefinitions
    verbs:
      - "*"
  - apiGroups:
      - ""
    resources:
      - pods
      - services
      - endpoints
      - events
      - configmaps
    verbs:
      - "*"
  - apiGroups:
      - ""
    resources:
      - secrets
    verbs:
      - "get"
  - apiGroups:
      - apps
    resources:
      - deployments
      - statefulsets
    verbs:
      - "*"
  - apiGroups:
      - policy
    resources:
      - poddisruptionbudgets
    verbs:
      - "*"
---
kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: "infra-redisoperator"
subjects:
  - kind: ServiceAccount
    name: "infra-redisoperator"
    namespace: infra
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: "infra-redisoperator"


---
# Source: redisoperator/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: "infra-redisoperator"
  namespace: infra
  labels:
    app.kubernetes.io/component: app
    app.kubernetes.io/name: "redisoperator"
spec:
  selector:
    app.kubernetes.io/component: app
    app.kubernetes.io/name: "redisoperator"
  ports:
  - name: metrics
    port: 9710
    protocol: TCP

---
# Source: redisoperator/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "infra-redisoperator"
  namespace: infra
  labels:
    app.kubernetes.io/component: app
    app.kubernetes.io/name: "redisoperator"
    helm.sh/chart: "redisoperator-3.1.0"
    app.kubernetes.io/instance: "infra"
    app.kubernetes.io/managed-by: "Tiller"
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: app
      app.kubernetes.io/name: "redisoperator"
  template:
    metadata:
      labels:
        app.kubernetes.io/component: app
        app.kubernetes.io/name: "redisoperator"
    spec:
      serviceAccountName: "infra-redisoperator"
      containers:
      - name: "redisoperator"
        image: "quay.io/spotahome/redis-operator:latest"
        imagePullPolicy: Always
        resources:
          limits:
            cpu: 100m
            memory: 50Mi
          requests:
            cpu: 10m
            memory: 50Mi
          
        ports:
        - containerPort: 9710
          name: metrics
          protocol: TCP

---
# Source: redisoperator/templates/monitoring.yaml

