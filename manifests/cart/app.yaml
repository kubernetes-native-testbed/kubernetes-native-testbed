---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart
  namespace: cart
  labels:
    service: cart
    role: app
spec:
  replicas: 3
  selector:
    matchLabels:
      service: cart
      role: app
  template:
    metadata:
      labels:
        service: cart
        role: app
    spec:
      containers:
        - name: cart
          image: registry-harbor-core.infra.svc.cluster.local/library/cart:64cfef480e6f61bc2eb5fb7edf27394776908292
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
              scheme: HTTP
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: cart
  namespace: cart
  labels:
    service: cart
    role: app
  annotations:
    contour.heptio.com/upstream-protocol.h2c: "8080"
spec:
  type: ClusterIP
  ports:
    - name: "http-port"
      protocol: "TCP"
      port: 8080
      targetPort: 8080
  selector:
    service: cart
    role: app
